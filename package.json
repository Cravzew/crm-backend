{
  "name": "crm-backend",
  "private": true,
  "main": "index.js",
  "devDependencies": {
    "axios": "^0.21.1",
    "jest": "^26.6.3"
  },
  "scripts": {
    "start": "npm run server:start && npm run client:start",
    "client:start": "npx live-server",
    "server:start": "node index"
  },
  "repository": {
    "type": "git"
  },
  "dependencies": {
    "live-server": "^1.2.2"
  }
}
